First version of a stack and 2 variants that are "concurrent" (lock based and lockless, a very lazy implementation at this stage, voluntarily).
To test & run (gitpod or unix/g++ environment):
g++ lfstack.cc -o lfstack && ./lfstack
